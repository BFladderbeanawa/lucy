// Package api exposes Lucy's stable embedded integration contract.
//
// The package is intentionally independent of Lucy's internal provider
// implementation so embedding consumers can plan, lock, and check status
// through a small DTO-based boundary.
package api
